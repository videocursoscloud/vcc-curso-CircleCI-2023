version: 2.1
orbs:
  aws-eks: circleci/aws-eks@2.2.0


commands:
  install-prereq-aws-cli:
    steps:
     - run: apt update && apt install curl unzip groff less -y


jobs:
  create-cluster:
    docker:
      - image: cimg/python:3.11.2
    steps:
#      - install-prereq-aws-cli
      - aws-eks/setup
      - aws-eks/create-cluster:
          config-file: whatever

workflows:
  cluster-management:
    jobs:
      - create-cluster:
          name: create cluster
          context: aws-access
#          config-file: whatever
             
