version: 2.1

jobs:
  saluda:
    docker: 
      - image: ubuntu:latest
    environment:
      NOMBRE: miguel
    steps:
      - run: echo hola $NOMBRE
      - run: env

workflows:
  workflow1:
    jobs:
      - saluda:
          context: miprueba
             
